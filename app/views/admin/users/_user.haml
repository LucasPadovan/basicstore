%tr{ id: dom_id(user) }
  %td= user.name
  %td
    = l user.created_at
    Hs.
  %td
    = srbuj_link_to_show ['admin', user]
    = srbuj_link_to_edit edit_admin_user_path(user)
    = srbuj_link_to_destroy ['admin', user], data: { target: dom_id(user) }, confirm: t('admin.users.index.confirm')