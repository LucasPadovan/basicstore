<%= form_for(@estadorden) do |f| %>
  <% if @estadorden.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@estadorden.errors.count, "error") %> prohibited this estadorden from being saved:</h2>

      <ul>
      <% @estadorden.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :order_id %><br />
    <%= :order_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= :user_id %>
  </div>
  <div class="field">
    <%= f.label :estado %><br />
    <%= f.text_field :estado %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
