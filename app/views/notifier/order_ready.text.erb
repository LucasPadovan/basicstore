Sr./Sra. <%= @order.name %>

Su orden de:
<% @total=0 %>
<% @order.line_items.each do |item| %>
    <% precio = item.total_price %>
<%= item.quantity %> x <%= item.product.titulo %> = <%= number_to_currency(precio) %>
<% @total+= precio %>
<% end %>

Total: <%= number_to_currency(@total) %>


Ya esta preparada y lista para ser entregada. Nos comunicaremos a la brevedad para establecer hora y punto de encuentro.

Muchas gracias.

Cristopher Smith Animestore.