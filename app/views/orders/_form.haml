= f.error_notification

= f.input :name, input_html: { limit: 40 }, autofocus: true
= f.input :address, input_html: { rows: 3, cols: 40 }
= f.input :email, input_html: { size: 40 }
-#todo: PaymentMethod.all mandar a helper
= f.input :payment_method_id, collection: PaymentMethod.all.map{|r|[r.name,r.id]}, prompt: t('.prompt')

= content_for :form_actions do
  = link_to_function 'Cerrar', "$('#modal').modal('toggle');", class: 'btn'
