%ul.main-nav.nav.nav-list
  %li= link_to t('.offers'), 'promociones_path'
  %li= link_to t('.new_products'), novedades_path
  %li= link_to t('.products'), store_path
  -#- @product_types.each do |t|
  -#  %li.submenu= link_to '| ' + t.nombre, productos_path( tipoprod: t.id )
-#= render partial: 'carts/cart'
