%ul.unstyled
  - @product_types.each_with_index do |type, i|
    %li.border{ class: RAINBOW_COLORS[i%RAINBOW_COLORS.size] }= link_to type, store_index_path( product_type: type.name ), class: current_subsection( store_urls(type) )
